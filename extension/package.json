{
  "name": "imagion-extension",
  "version": "0.1.0",
  "description": "Imagion browser extension (TypeScript sources + build tools)",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.234",
    "typescript": "^5.5.2"
  }
}
